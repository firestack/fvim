{
  # TODO: Implement general mappings
  plugins.which-key = {
    enable = true;
    settings.icons = {
      breadcrumb = "»";
      group = "+";
      separator = ""; # ➜
    };
    # registrations = {
    #   "<leader>t" = " Terminal";
    # };
  };
}
