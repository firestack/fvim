{
  # TODO: Implement general mappings
  plugins.which-key = {
    enable = true;
    icons = {
      breadcrumb = "»";
      group = "+";
      separator = ""; # ➜
    };
    # registrations = {
    #   "<leader>t" = " Terminal";
    # };
  };
}
